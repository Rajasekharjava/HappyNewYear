server:
  port: 9001
  
spring:
  application:
    name: SpringCloudRibbonLoadBalancer
 
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9090/eureka/
      fetch-registry: true
      register-with-eureka: true

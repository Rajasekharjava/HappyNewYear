server:
  port: 8001
  
spring:
  application:
    name: EurekaClientConsumer
 
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9090/eureka/
      fetch-registry: true
      register-with-eureka: true
